using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class FactoryVarianceSO<T> : ScriptableObject, IFactoryVariance<T>
{
    public abstract T Create(T key);
    public abstract Dictionary<T, Stack<T>> CreateBatch(int copiesPerItem);
}
